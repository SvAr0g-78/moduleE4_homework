'use strict';

/* Написать функцию, которая создает пустой объект, но без прототипа. */

function  emptyObj() {
    console.log('123')
    const new_obj = new Object();
    return new_obj;
}

// let newObj = emptyObj();
// newObj.a = 12;
// newObj.b = 34;
